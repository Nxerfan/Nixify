# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel


# typescript
*.tsbuildinfo
next-env.d.ts
local-*
.claude
.z-ai-config
*.log
dev.log
dev.out.log
test
prompt

server.log
# Skills directory
/skills/

# Phase 11 audit (BLOCKER #9): generated agent artifacts — never commit.
/tool-results/

# Local SQLite database (used by tests + dev sandbox). DO NOT commit —
# contains seeded test data. Production uses PostgreSQL via DATABASE_URL.
db/custom.db
db/custom.db-journal
db/custom.db-wal
db/custom.db-shm

# Windows reserved names
nul
NUL
Nul
NUl
agent-ctx/
